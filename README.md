# Veryard Portfolio

Next.js static portfolio designed for GitHub Pages.

## Run locally

```bash
npm install
npm run dev
```

## GitHub Pages

Set repository Settings → Pages → Source to **GitHub Actions**. Push to `main` and the workflow will build and deploy the `out` folder.
